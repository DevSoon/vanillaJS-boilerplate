const NotFoundPage = () => {
    const element = document.createElement('div');
    element.innerHTML = `
        <header>
            <h1>NotFoundPage</h1>
        </header>
    `;

    return element;
};

export default NotFoundPage;
